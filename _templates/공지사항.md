<%*
const authorMap = {
  "beomjinkim2000": "김범진(PM)",
  "YOUR_GITHUB_ID_B": "팀원B(Exp)",
  "YOUR_GITHUB_ID_C": "팀원C(Model)",
  "YOUR_GITHUB_ID_D": "팀원D(Data)"
};

const configPath = "_config/me.md";
let gitUser = "";
try {
  const f = app.vault.getAbstractFileByPath(configPath);
  if (f) gitUser = (await app.vault.read(f)).split("\n")[0].trim();
} catch(e) {}

if (!gitUser) {
  gitUser = ((await tp.system.prompt("GitHub ID를 입력하세요 (한 번만 저장됩니다)")) || "").trim();
  // 빈 값이면 저장하지 않는다 — 빈 me.md가 생기면 이후 영영 다시 묻는 루프에 빠진다
  if (gitUser) {
    const meBody = gitUser + "\n\n> 게시판 글쓴이 자동 기록용 개인 설정 파일입니다 (1행 = 내 GitHub ID).\n> 이 파일을 지우면 새 글을 쓸 때마다 GitHub ID를 다시 물어봅니다.\n";
    try {
      if (!app.vault.getAbstractFileByPath("_config")) await app.vault.createFolder("_config");
      const existing = app.vault.getAbstractFileByPath(configPath);
      if (existing) await app.vault.modify(existing, meBody);
      else await app.vault.create(configPath, meBody);
    } catch(e) { console.error("[공지 템플릿] me.md 저장 실패:", e); }
  }
}

const author = authorMap[gitUser] || gitUser || "익명";

// 같은 날 공지가 이미 있으면 rename이 예외로 죽어 '제목 없음' 노트로 남는다 → 시간을 붙여 회피
let noticeName = tp.date.now("YYYY-MM-DD") + "-공지";
if (app.vault.getAbstractFileByPath("전체 공지사항/" + noticeName + ".md")) noticeName += " " + tp.date.now("HHmm");
await tp.file.rename(noticeName);
-%>
---
글쓴사람: <% author %>
날짜: <% tp.date.now("YYYY-MM-DD HH:mm") %>
type: notice
---
<div style="display:flex;align-items:center;gap:12px;margin:16px 0"><hr style="flex:1;margin:0"><span style="font-weight:bold;white-space:nowrap">내용</span><hr style="flex:1;margin:0"></div>

<% tp.file.cursor() %>

<div style="display:flex;align-items:center;gap:12px;margin:16px 0"><hr style="flex:1;margin:0"><span style="font-weight:bold;white-space:nowrap">대상</span><hr style="flex:1;margin:0"></div>

- [ ] 전체
- [ ] 팀원A(PM)
- [ ] 팀원B(Exp)
- [ ] 팀원C(Model)
- [ ] 팀원D(Data)
- [ ] 없음

<div style="display:flex;align-items:center;gap:12px;margin:16px 0"><hr style="flex:1;margin:0"><span style="font-weight:bold;white-space:nowrap">댓글</span><hr style="flex:1;margin:0"></div>

