---
tags: [rag, retrieval, experiment, result, embedding, reranker, bm25, goldenset200]
created: 2026-07-29
updated: 2026-07-29
status: 실행 중
---

# 검색 2차 실험 결과 (임베딩 교체)

> 계획·측정 조건은 [[검색 2차 실험 계획 (임베딩·BM25·리랭커)]] **§0-1이 정본**이다.
> 1차 판정 → [[검색 실험 결과 (164문항)]] · 개념이 낯설면 → [[개념 색인]]

> [!success] 여기까지 왔다 (2026-07-29)
> **6.8.5 dense 단독** · **6.8.6 리랭커** · **6.8.7 편향 상·하한 + paired bootstrap** — 완료
> **6.8.8 전수 증분 라벨링** — 라벨링 4샤드 완료(42/2,105), **독립 검증 진행 중** → §8-6
>
> **확정: BM25 최종 기각 · R2 기각(R1 유지).**
> **미확정: 임베딩 교체(KURE) 승격 여부 — 전수 재채점 후 6.8.9에서 판정.** → §8-7
>
> 1·2부의 수치는 전부 **하한 채점값**이고, 라벨이 확정되면 재산정된다.
> 이 문서의 모든 수치는 **골든셋 `v4_20260729` 기준**이다.

---

## 0. 세 줄 요약

1. **dense 단독으로는 새 임베딩이 크게 이긴다** — MRR 0.6402 → 0.7417/0.7416 (**+0.10**).
2. **그런데 리랭커를 켜면 격차가 +0.0068로 줄고**, 95% 신뢰구간 **[−0.029, +0.043]**이 0을 품는다. 계획 §14의 예상 2번이 그대로 맞았다.
3. **그 +0.0068이 정답지 편향 탓인지 확인하려고 미판정 청크를 직접 읽고 있다.** 신규 정답률은 2.0%로 낮지만 **점수는 그보다 크게 움직인다** — 42개 중 7개만 반영해도 KURE가 +0.0078 올랐다. **판정은 전수 재채점 후.** → §8

---

# 1부 · 성적표

## 1. dense 단독 — 6.8.5

164문항, `C_doc` 스코프, 문서범위. 리랭커 없이 임베딩 실력만 본 값이다.

| | 임베딩 | [[MRR]]@10 | +[[BM25]] | [[nDCG]]@10 | [[Recall과 hit rate\|Recall]]@5 | Recall@20 | 정답이 후보에 아예 없음 |
|---|---|---|---|---|---|---|---|
| **M0** | `multilingual-e5-small` (118M/384d) | 0.6402 | 0.6096 | 0.6061 | 0.6188 | 0.8163 | **17** |
| **M1** | `BAAI/bge-m3` (568M/1024d) | **0.7417** | 0.6179 | 0.6973 | 0.7132 | 0.8557 | 14 |
| **M2** | `nlpai-lab/KURE-v1` (0.6B/1024d) | **0.7416** | 0.6491 | 0.7082 | 0.7335 | **0.8631** | **13** |

읽을 것 셋.

**① BM25는 세 임베딩 모두에서 순손해다.** 계획 §5에서 "dense가 약해지면 BM25가 살아날 수도 있다"고 열어뒀던 축인데, 세 조건 전부에서 닫혔다.

**② dense가 강할수록 BM25 손해가 커진다.** M0에서 −0.031이던 것이 M1에서 **−0.124**다. §5의 가설("dense가 훨씬 강하면 노이즈만 섞인다")이 방향까지 맞았다 — 다만 결론은 살아나는 쪽이 아니라 **더 확실히 죽는 쪽**이었다.

**③ M1 ≈ M2.** 0.7417 vs 0.7416, 소수점 넷째 자리에서 뒤집히는 차이다. **dense 단계에서 한국어 튜닝의 이득은 없다.** 계획 §3의 판정표대로 읽으면 **"계열이 관건"**이다.

> **M0의 Recall@20 = 0.8163이 1차 실험값과 정확히 재현됐다.** 캐시를 새로 만들고 채점 경로를 갈아탄 뒤에도 같은 수가 나왔다는 뜻이라, 이 표 전체를 믿을 근거가 된다.

**후보 밖 문항이 17 → 13으로 줄었다.** 리랭커로는 영원히 못 고치는 문항이 4개 줄어든 것이고, 계획 §2-3이 이 실험의 명분으로 든 바로 그 지표다. 다만 여전히 13문항이 남는다.

## 2. 리랭커까지 — 6.8.6

`C_doc` 종단 MRR@10. **판정용 지표**다(계획 §7).

| 임베딩 | dense 단독 | + **R1** `bge-reranker-v2-m3` | + **R2** `bge-reranker-v2-m3-ko` |
|---|---|---|---|
| M0 e5-small | 0.6402 | **0.7443** | 0.6805 |
| M1 bge-m3 | 0.7417 | 0.7412 | 0.6580 |
| M2 KURE-v1 | 0.7416 | **0.7511** | 0.6702 |

**가장 높은 칸은 M2 × R1 = 0.7511.** 기준선(M0 × R1 = 0.7443) 대비 **+0.0068**이다.

> [!warning] 이 +0.0068을 개선폭으로 읽지 말 것
> 95% 신뢰구간이 **[−0.029, +0.043]**으로 0을 넉넉히 품는다. **우연 범위다** → §7-2.
> 그리고 이 표는 미판정 청크를 전부 오답으로 세는 **하한**이라 M1·M2가 눌려 있다(§7-1).
> **얼마나 눌려 있는지를 지금 라벨링으로 재고 있다** → §8. 승격 판정은 그 뒤다.

> [!check] 재현성 확인 — M0 × R1이 소수점 넷째 자리까지 1차와 같다
> 0.7443. 임베딩 캐시를 새로 만들고 리랭킹 스크립트를 고친 뒤에도 같은 값이 나왔다.
> **측정 파이프라인이 1차와 동일하게 돈다**는 뜻이고, 나머지 다섯 칸을 1차와 나란히 비교할 수 있는 근거다.

---

# 2부 · 세 가지 발견

## 3. 임베딩 교체 이득이 리랭커에 흡수된다 ★

이 실험의 핵심 결과다.

```
dense 단독:   M0 0.6402  →  M2 0.7416      +0.10   ← 임베딩 실력 차이는 크다
+ 리랭커 R1:  M0 0.7443  →  M2 0.7511      +0.0068 ← 그런데 종단에서는 거의 안 남는다
```

**리랭커가 이미 그 몫을 하고 있었다.** M0가 20위 안에 어수선하게 담아온 후보를 리랭커가 제대로 줄 세워버리므로, 임베딩을 키워서 "미리 잘 줄 세워 오는" 이득은 중복 투자가 된다.

계획 §2-2가 나눠 적어둔 두 능력으로 읽으면 정확히 맞아떨어진다.

| 임베딩을 바꿔서 얻는 것 | 예상 | 실측 |
|---|---|---|
| 정답을 20개 안에 **넣는** 능력 | 리랭커가 못 함 → 값어치 크다 | Recall@20 0.8163 → 0.8631, 후보 밖 17 → 13 ✅ |
| 20개 안에서 **위로 올리는** 능력 | 리랭커가 이미 함 → 작다 | 종단 +0.0068에 그침 ✅ |

계획 §14의 예상 1·2번이 둘 다 맞았다. 그리고 §14가 미리 적어둔 대로, **이건 실패가 아니라 "리랭커가 이미 그 몫을 하고 있다"는 유용한 결론**이다.

> 단, "흡수됐다"는 이 서술은 **하한 채점 위에서** 성립한다.
> 라벨을 더 붙일수록 M1·M2만 점수가 오르므로 **흡수의 크기는 정답지에 달려 있다** → §8.

## 4. 한국어 튜닝 리랭커 R2는 전 조합에서 크게 진다

R2가 M0·M1·M2 어디에 붙어도 R1보다 **−0.06 ~ −0.08**이다. 근소한 차이가 아니라 명백한 패배다.

원인을 실측으로 잡았다 — **R2는 점수가 1.0으로 포화한다.**

| 한 문항의 R2 점수 | |
|---|---|
| 후보 20개 중 고유 점수 | **10개뿐** (나머지는 동점) |
| 상위 3개 점수 | **전부 정확히 1.0** |

점수가 다 같으면 순위를 매길 수 없다. **변별을 못 하는 것**이다([[지표 포화]]와 같은 종류의 현상이 모델 출력 쪽에서 일어난 셈).

**결론: R1을 유지한다.** 리랭커 교체 축은 여기서 닫는다.

> §3의 M1≈M2와 합치면, **임베딩과 리랭커 양쪽에서 한국어 추가 학습판이 원본을 못 이겼다.**
> 계획 §4가 "둘 다 미미하면 우연이 아니라 패턴"이라고 미리 적어둔 그 자리다.
> 다만 방향은 예상(§14-5 "근소하다")보다 나빴다 — 리랭커 쪽은 근소한 게 아니라 크게 졌다.

## 5. C ≡ D 불일치 1건 — 버그가 아니다

**M0 × R2에서만** `C_doc` 0.6805 vs `D_doc` 0.6815로 **0.001** 어긋났다.
계획 §6이 *"확인용 6조합이 다르면 결과가 아니라 버그 신호"*라고 못박아 둔 자리라, 멈추고 봤다.

**버그가 아니었다. §4의 점수 포화가 만든 것이다.**

C와 D는 후보 **집합은 같고 순서만 다르다**([[검색 실험 결과 (164문항)]] §4). 리랭커가 그 순서를 자기 점수로 덮어쓰기 때문에 보통은 결과가 같아진다. 그런데 **점수가 동점이면 안정 정렬이 입력 순서를 그대로 유지**한다.

```
C: [dense 순서]         → R2 점수 [1.0, 1.0, 1.0, ...] → 동점 → 입력 순서 유지
D: [BM25로 섞인 순서]   → R2 점수 [1.0, 1.0, 1.0, ...] → 동점 → 입력 순서 유지
                             ↑ 순서를 지우지 못한다
```

164문항 중 **1문항**(`g200:4406b63d`)에서 정답 청크가 C는 3등, D는 2등이 됐다. 그게 0.001이다.

**R1에서는 한 건도 발생하지 않았다.** R1은 점수가 포화하지 않아 순서를 제대로 덮어쓴다.
즉 이 불일치는 "D≡C가 깨졌다"가 아니라 **R2를 쓰면 안 되는 이유가 하나 더 나온 것**이다.

---

# 3부 · 하다가 바로잡은 것

## 6. 검색과 재정렬이 서로 다른 질문을 보고 있었다 ★

리랭킹을 처음 돌렸을 때 **0.7747**이라는 값이 나왔다. 기준선 대비 +0.03을 훌쩍 넘는 수라 승격처럼 보였는데, **가짜 상승이었다.**

원인은 질문 파일이었다.

| 단계 | 무엇을 봤나 |
|---|---|
| 1단계 — dense 후보 캐시 | `qid_query_200.jsonl` (**병합본**) |
| 2단계 — 리랭킹 | `goldenset_200.jsonl` (**원본**) ← 여기가 틀렸다 |

두 파일은 **후속 질문 29문항의 텍스트가 다르다.**

```
원본:    "그 사업의 안정화기간은?"
병합본:  "한영대학교 …사업기간은 어떻게 되나요? 그 사업의 안정화기간은?"
```

그래서 **검색은 맥락이 붙은 질문으로 후보를 뽑았는데, 리랭커는 "그 사업"이 뭔지 모르는 채로** 그 후보 29문항어치의 순서를 매겼다. 그 어긋남이 0.7747이라는 상승으로 나타났다.

**골든셋·정답지·채점 기준에는 문제가 없었다.** 잘못은 전적으로 실행 인자 쪽이다.

> [!danger] 교훈 — 1단계(후보 캐시)와 2단계(리랭킹)는 반드시 **같은 질문 파일**을 봐야 한다
> 이것도 [[검색 실험 결과 (164문항)]] §8(인덱스 혼동)과 같은 **조용한 실패**다.
> 파일 이름이 둘 다 그럴듯하고, 문항 수도 맞고, 에러도 안 난다. **성적이 올라서 발견됐다.**
> 성적이 내려갔으면 "새 임베딩이 별로네"로 넘어갔을 수도 있다.

### 6-1. 두 질문 파일의 관계

| 파일 | 무엇인가 | 필드 | 문항 |
|---|---|---|---|
| `goldenset_200.jsonl` | **정본 골든셋** | `qid` / `question` / 유형 / `선행_턴` / `대상_doc_id` / `expected_answer_기준` | 200 |
| `qid_query_200.jsonl` | 위에서 파생된 **검색 실험용 질문 텍스트** | `qid` / `query` **둘뿐** (정답 정보 없음) | **164** |
| `rerank_labels_200.jsonl` | **정답 라벨**을 따로 들고 있다. VM `v4_20260729`의 `rerank_labels.jsonl`과 **gold 청크 전건 일치**(로컬엔 `유형` 필드만 없음) | | 164 |

```
goldenset_200.jsonl (200문항)
    │  선행_턴 + question 을 이어붙임
    │  − 모른다 15 − 반문 15 − 빈 정답 6
    ▼
qid_query_200.jsonl (164문항, qid + query)   ← 검색·리랭킹은 항상 이걸 본다
```

**정답 정보는 `qid_query_200.jsonl`에 없다.** 채점은 `rerank_labels_200.jsonl`이 맡는다.
`goldenset_200.jsonl`을 검색 인자로 넣으면 문항 수 필터링과 선행 턴 병합이 **둘 다** 빠진다.

---

# 4부 · 다음

## 7. 정답지 편향을 구간에 가뒀다 — 6.8.7 ★

계획 §9가 **최대 위험**으로 지목해둔 것을 실제로 쟀다. 결론부터 — **1·2부의 판정이 뒤집히지는 않았지만, "판정할 수 없다"로 바뀌었다.**

### 7-1. 정답지는 M0가 만들었다 — 얼마나 불리한가

정답 라벨은 **e5(M0)가 뽑아온 후보만 사람이 판정**해서 만들어졌다. 새 모델이 그 풀 밖에서 진짜 정답을 찾아오면 라벨이 없어 **오답으로 세어진다.**

리랭킹 **상위 10개** 기준 미판정 비율 실측:

| 조합 | 상위 10개 중 미판정 |
|---|---|
| `m0_r1` e5 + R1 (현행) | **0.7%** |
| `m1_r1` bge-m3 + R1 | **26.0%** |
| `m2_r1` KURE + R1 | **32.0%** |

**새 모델이 사용자에게 보여줄 10개 중 3분의 1을 아무도 읽어본 적이 없다.** 그리고 현행 채점은 그걸 전부 오답으로 센다. 계획 §9의 *"잘할수록 손해를 본다"*가 수치로 나타난 것이다.

그래서 [[상·하한 채점]]으로 세 번 채점했다.

| 이름 | 미판정 청크를 | 성격 |
|---|---|---|
| **하한** | 오답으로 센다 | 현행 채점. 새 모델에 가장 가혹 |
| **응축** | 순위에서 빼고 센다 | 판정된 것끼리만 겨룸 |
| **상한** | 정답으로 센다 | 새 모델에 가장 관대 |

`하한 ≤ 응축 ≤ 상한`이 항상 성립한다. `C_doc` MRR@10:

| 조합 | 하한 | 응축 | 상한 | 폭 |
|---|---|---|---|---|
| `m0_r1` e5+R1 | 0.7443 | 0.7446 | 0.7461 | **0.0018** |
| `m0_r2` e5+R2 | 0.6805 | 0.6805 | 0.6805 | 0.0000 |
| `m1_r1` bge-m3+R1 | 0.7412 | 0.7546 | 0.7916 | 0.0504 |
| `m1_r2` bge-m3+R2 | 0.6581 | 0.6684 | 0.7133 | 0.0552 |
| `m2_r1` KURE+R1 | 0.7511 | 0.7639 | 0.8002 | **0.0491** |
| `m2_r2` KURE+R2 | 0.6702 | 0.6791 | 0.7350 | 0.0648 |

> **M0의 폭이 0.0018로 사실상 없다.** 이게 두 가지를 동시에 말해준다 —
> ① **정답지가 M0에 맞춰져 있다**는 직접적인 증거이고,
> ② 그러므로 **1차 실험 수치는 그대로 신뢰해도 된다**는 뜻이다.

### 7-2. paired bootstrap 95% 신뢰구간

164문항 복원추출 **1만 회**, 시드 `20260729` 고정, 기준선 `m0_r1` 대비.
문항별 차이 `d_i = 도전점수(i) − 기준점수(i)`를 리샘플한다 → [[paired bootstrap]]

| 조합 | 현행 채점 (하한↔하한) | **보수** (도전 하한↔기준 상한) | **낙관** (도전 상한↔기준 하한) | 판정 |
|---|---|---|---|---|
| `m0_r2` | −0.0638 [−0.1005, −0.0275] ✱ | −0.0657 ✱ | −0.0638 ✱ | **기각** |
| `m1_r1` | −0.0030 [−0.0414, +0.0364] | −0.0049 [−0.0434, +0.0347] | **+0.0473 [+0.0106, +0.0862] ✱** | **라벨링 필요** |
| `m1_r2` | −0.0862 [−0.1358, −0.0377] ✱ | −0.0881 ✱ | −0.0310 [−0.0778, +0.0169] | **기각** |
| `m2_r1` | **+0.0068 [−0.0290, +0.0433]** | +0.0050 [−0.0309, +0.0421] | **+0.0559 [+0.0197, +0.0935] ✱** | **라벨링 필요** |
| `m2_r2` | −0.0741 [−0.1216, −0.0272] ✱ | −0.0760 ✱ | −0.0093 [−0.0569, +0.0396] | **기각** |

<small>✱ = 95% 구간이 0을 포함하지 않음</small>

판정 규칙은 세 갈래다.

```
보수 검정이 0 위        → 채택 (가장 불리하게 봐도 이긴다)
낙관 검정마저 0을 품음  → 기각 (라벨을 더 붙여도 못 이긴다)
그 사이                 → 라벨링이 판정을 좌우한다
```

### 7-3. 그래서 뭐가 확정됐나

**① 앞서 "가장 높은 칸"으로 적었던 KURE 우위(+0.0068)는 우연 범위다.**
구간 [−0.029, +0.043]이 0을 넉넉히 품는다. 지금 데이터로 **"더 낫다"고 말할 근거가 없다.**
계획 §13이 신뢰구간을 주 기준으로 삼은 이유가 여기서 그대로 작동했다 — 점추정만 봤으면 또 §5의 실수를 반복할 뻔했다.

**② 그렇다고 기각도 못 한다.** 낙관 검정에서는 M1 +0.047, M2 +0.056으로 **둘 다 구간이 0 위**다.
즉 **"실력 차이가 없다"가 아니라 "우리 정답지로는 잴 수 없다"**가 정확한 표현이다.

**③ R2는 어떤 채점 가정으로도 진다.** §4의 결론이 이제 통계적으로도 확정됐다 — **R1 유지**.

### 7-4. 이 보류를 어떻게 푸나

여기까지가 **"채택도 기각도 못 한다"**로 끝난 상태다. 판정을 가르는 건 통계가 아니라 **미판정 청크에 진짜 정답이 얼마나 있느냐**이고, 그건 재는 수밖에 없다.

**그래서 실제로 읽고 있다 → §8.** 상한 0.8002가 과대였다는 것까지는 확인됐고, **그 아래 어디쯤인지**를 지금 좁히는 중이다.

### 7-5. 산출물

| | |
|---|---|
| `src/eval/label_bias.py` | 3단 채점(하한·응축·상한) + paired bootstrap |
| `scripts/rerank_bias_bounds.py` | 3갈래(현행·보수·낙관) 판정 |
| `scripts/rerank_on_cache.py --dump-ranked` | 순위 목록 저장 — **chunk_id만 담는다**(NDA) |
| `data/bias_bounds_200.json` | 결과 |

pytest **12건 추가**(라벨 전무·전부 경계, 시드 고정 재현성) 포함 **전체 515건 통과**, lint 클린.

## 8. 미판정 청크를 실제로 읽고 있다 — 6.8.8 ★

§7이 남긴 보류를 풀려면 **미판정 청크에 진짜 정답이 얼마나 있는지**를 재는 수밖에 없다. 시범 샤드를 읽어보니 **정답률은 낮았다(1.3%).** 그런데 **그걸 "영향이 없다"로 읽은 것이 이 문서가 한 번 틀린 자리다** → §8-4.

지금은 **전수(2,105개) 라벨링을 마치고 독립 검증 중**이며, 판정은 그 뒤다.

### 8-1. 설계 — 정답지는 하나로 둔다

모델별로 정답지를 만들면 **서로 다른 시험**이 되어 비교 자체가 불가능해진다. 그래서 정답지는 하나로 두고, **세 임베딩의 `dense_doc` 후보를 합집합**으로 묶어 판정 풀을 넓혔다. 기존 판정분은 재사용하고 **증분만** 판정한다.

| | 값 |
|---|---|
| 합집합 | 문항당 **36.0개** (최소 25 / 최대 50) |
| 신규 판정 대상 | **2,105개** (문항당 12.8) |

**범위를 `dense_doc`만으로 잡았다.** BM25는 미채택이고 `dense_full`은 프로덕션 경로가 아니라, 판정 기준인 `C_doc` 점수에 영향을 주지 않는다(전 필드로 넓히면 2,496개) → [[후보 캐시]]

**기존 파이프라인을 그대로 썼다** — 라벨러 `rounds/r8/SKILL.md`(v8, 재현율 0.863 / 정밀도 0.750) → 독립 검증자 `VERIFY_SPEC.md`. 200문항 골든셋이 만들어진 바로 그 흐름이다([[골든셋]]).

> [!important] 여기서는 **절대 정확도보다 일관성**이 중요하다
> 증분을 더 엄격한 방법으로 라벨링하면 기존 정답과 잣대가 갈린다.
> 그러면 e5(기존 라벨 위주)와 KURE(신규 라벨 위주)가 **서로 다른 기준으로 채점**된다.
> **지금 고치려는 편향을 다른 편향으로 바꾸는 꼴**이다. 그래서 같은 스킬·같은 검증 절차를 쓴다.

### 8-2. shard1 결과 — 43문항 / 판정 대상 530개

| | |
|---|---|
| 신규 정답 | **7개 / 530개 = 1.3%** |
| 기존 판정 풀의 정답률 | 310 / 6,751 = **4.6%** (**3.5배**) |
| 신규 정답 0개인 문항 | 43문항 중 **37개** |
| confidence | 높음 29 · 보통 14 |
| 기계검사 | 지어낸 id **0건** · confidence 규칙 위반 **0건** |

### 8-3. 정답률이 낮아 라벨러를 의심했다 — 검증자를 붙였다

1.3%는 기존의 1/3.5다. **"덜 고른 것 아닌가"**를 의심할 만한 신호가 셋 있었다.

1. 정답률이 기존의 **1/3.5**
2. **R8**(쪼개진 표를 한 단위로 묶는 규칙, 우선순위 3위) 적용이 43문항 **전부 0건** — RFP는 표가 청크 경계에서 갈리는 일이 흔한데도
3. 기존 정답 청크와 **번호가 바로 인접한**(`::NNNN` ±1) 신규 후보 25개 중 정답 판정은 **2개**뿐

`VERIFY_SPEC`에 *"라벨러는 대개 덜 고르는 쪽으로 틀린다 — 놓친 정답이 초과 오답보다 많았다"*가 명시돼 있어, **독립 검증자에게 530개를 전부 재독**시켰다.

| verdict | |
|---|---|
| 통과 | **40** |
| 수정 | 3 |
| 재작업 | 0 |
| **추가 / 제거** | **0건 / 0건** |
| 보류 | 3 |

**최종 정답은 7개 그대로.** 530개를 전부 다시 읽고도 추가 정답이 **한 건도** 나오지 않았다.

의심 신호 셋은 전부 해소됐다. 인접 청크 25개 중 **23개의 배제 사유가 원문 인용과 함께 남아 있고**(예: 구축일정 간트표에 "안정화" **행 이름만 있고 값이 없음**), 문항 간 일관성도 확인됐다(`g200:0160267c`와 `g200:af85ee75`에서 같은 유형을 동일하게 배제).

> **낮은 정답률은 라벨러의 태만이 아니라 실제 분포였다.**
> 검증자를 붙이지 않았으면 이 결론을 신뢰할 수 없었다 — 의심 신호가 있을 때 재독시킨 게 값을 했다.

### 8-4. 여기서 "미채택"으로 판단했다가 **철회했다** ★

이 시점에서 이렇게 적었다.

> ~~상한 가정이 비현실적이었다. 실측 정답률이 1.3%뿐이니 **하한이 거의 참값**이고,
> 하한 기준 구간 [−0.029, +0.043]이 0을 품으니 **임베딩 교체는 승격 근거가 없다.**~~

**틀렸다.** 정답률만 보고 **그 정답이 점수를 얼마나 움직이는지는 재지 않은** 판단이었다.

찾은 7개를 실제로 채점에 반영해봤다(43문항분만).

| 조합 | 반영 전 | 반영 후 | Δ |
|---|---|---|---|
| e5 + R1 (현행) | 0.7443 | 0.7443 | **+0.0000** |
| bge-m3 + R1 | 0.7412 | 0.7473 | +0.0061 |
| **KURE + R1** | 0.7511 | 0.7590 | **+0.0078** |

**7개뿐인데 KURE가 +0.0078 올랐다.** 그 7개가 6문항에 붙었고, [[MRR]]은 정답 하나가 한 문항 점수를 0 → 0.5로 바꿔버리는 지표다. e5가 꿈쩍도 안 한 건 예상대로다 — 기존 라벨이 애초에 e5 후보에서 나왔으니 더 얻을 게 없다(§7-1).

> [!danger] 교훈 — **정답률이 낮은 것과 지표에 영향이 없는 것은 다른 얘기다**
> [[MRR]]은 상위 순위에 가혹하게 민감하다. 2,105개 중 42개(2.0%)라는 **개수 비율**은
> "무시해도 되는 크기"처럼 보이지만, 그게 어느 문항 몇 등에 꽂히느냐에 따라 지표는 크게 움직인다.
>
> **판정은 개수 비율이 아니라 지표에 반영해서 봐야 한다.** 이 문서가 한 번 틀렸던 자리다.

### 8-5. 전수로 외삽하면 유의성 경계에 걸린다

7개 반영 상태의 신뢰구간을 다시 쟀다. 아직 0을 품는다.

| 조합 | Δ (7개 반영) | 95% CI |
|---|---|---|
| KURE + R1 | +0.0147 | [−0.0206, +0.0510] |
| bge-m3 + R1 | +0.0030 | [−0.0351, +0.0416] |

문제는 **이게 43문항분 라벨만 반영한 값**이라는 것이다. 표본 43문항 중 **6문항**이 정답을 얻었으므로, 전수 164문항이면 약 **23문항**이 같은 일을 겪는다 → KURE 상승분 **약 +0.0297** 추정.

```
기준선 대비 격차 :  +0.0068  →  약 +0.037 (전수 외삽)
신뢰구간 폭      :  ±0.036
```

**정확히 경계에 걸린다.** 그래서 표본에서 끊으면 판정문이 "모르겠다"로 끝난다 —
그게 아래 전수 확대 결정의 이유다.

### 8-6. 전수 판정으로 확대했다 — 라벨링 4샤드 완료

사용자 결정으로 표본에서 멈추지 않고 **2,105개 전수**로 갔다.

| 샤드 | 문항 | 신규 정답 / 판정 | 비율 |
|---|---|---|---|
| shard1 | 43 | 7 / 530 | 1.3% |
| shard2 | 42 | 13 / 525 | 2.5% |
| shard3 | 37 | 13 / 525 | 2.5% |
| shard4 | 42 | 9 / 525 | 1.7% |
| **합계** | **164** | **42 / 2,105** | **2.0%** |

지어낸 chunk_id **0건**, 스키마 위반 **0건**.

| | |
|---|---|
| shard1 | ✅ 검증 완료 (추가 0 · 제거 0 · 보류 3) |
| shard2·3·4 | ⏳ **독립 검증 진행 중** |

> 샤드별 1.3~2.5% 편차는 **라벨러가 넷이라 잣대가 갈렸을 가능성**이 있다.
> 각 검증자에게 *"잣대 차이인지 구성 차이인지 판단하라"*고 명시적으로 물어둔 상태다.
> §8-1의 **일관성이 절대 정확도보다 중요하다**는 원칙이 걸린 자리다.

### 8-7. 지금 확정된 것과 아닌 것

| | |
|---|---|
| ✅ **확정** | **신규 후보의 정답률은 낮다** — shard1 1.3%, 전수 2.0%. 독립 검증 통과(추가 0 · 제거 0) |
| ✅ **확정** | **6.8.7의 상한 0.8002는 과대**였다. 미판정을 전부 정답으로 치는 가정은 실제와 멀다 |
| ✅ **확정** | **R2 리랭커 기각 · R1 유지** — 세 채점 가정 어디서도 유의하게 진다 (§4·§7-2) |
| ✅ **확정** | **BM25 미채택** (§1) |
| ⏸ **미확정** | **KURE 승격 여부.** 전수 외삽이 유의성 경계에 걸려 **지금은 답이 없다** |

**최종 판정은 검증 → 병합 → 재채점이 끝나야 나온다.**

### 8-8. 남은 것

- shard2·3·4 **독립 검증 진행 중** → 병합 후 전수 재채점 → 6.8.9 판정
- **보류 3건 미해결**(shard1) — 사람 확인 대상. 가장 날카로운 건 `g200:15de45f1`로, 표에 "계약일로부터 3개월 / 2024년 11월 1일까지"가 있고 기준답변은 "계약일로부터 90일"이라 **3개월 = 90일을 같은 값으로 볼지**가 쟁점이다(R6 표기 정규화)
- 라벨이 최종 확정되면 골든셋은 **v5**가 되고, **e5 점수를 포함한 모든 수치가 재산정**된다. 그때 옛 수치는 지우지 말고 **"v4 기준"**으로 표기해 남긴다

## 9. 축별 상태

| 축 | 판정 | 근거 |
|---|---|---|
| **BM25** | ❌ **최종 기각** | 임베딩 3종 전부에서 순손해. dense가 강할수록 더 크게 진다 → §1 |
| **리랭커 모델** | ✅ **R1 유지 확정** | R2는 전 조합 −0.06~−0.08, 어떤 채점 가정으로도 진다 → §4·§7-2 |
| **한국어 튜닝** | 임베딩 쪽 **이득 없음**(M1≈M2) / 리랭커 쪽 **손해**(R2 ≪ R1) | → §1·§4 |
| **임베딩 교체** | ⏸ **판정 대기 — 전수 라벨링 진행 중** | 하한 +0.0068이나 라벨을 반영할수록 오른다. 전수 외삽이 유의성 경계 → §8 |

---

## 10. 관련 문서

| 문서 | 무엇을 보러 |
|---|---|
| [[검색 2차 실험 계획 (임베딩·BM25·리랭커)]] | **§0-1 = 측정 조건 정본** · 설계 근거 · 승격 기준(§13) |
| [[검색 실험 결과 (164문항)]] | 1차 판정 — 이 실험의 기준선(M0×R1 = 0.7443) |
| [[골든셋]] | 164문항 정답지가 어떻게 만들어졌나 (§7의 편향 출처) |
| [[후보 캐시]] | 왜 검색을 한 번만 돌리나 |
| [[상·하한 채점]] | 라벨 없는 청크를 어떻게 다루나 |
| [[paired bootstrap]] | 신뢰구간을 왜 쓰나 |
| [[개념 색인]] | MRR·nDCG·BM25·리랭커 개념 |

**코드**: `scripts/build_retrieval_cache.py` · `scripts/rerank_on_cache.py` · `scripts/rerank_bias_bounds.py` · `src/eval/label_bias.py` · `scripts/promote_decision.py`
**라벨링 스킬**: `.harness-mem/goldenset-skill/rounds/r8/SKILL.md`(v8) · `VERIFY_SPEC.md`
**정본**: `Plans.md` 6.8.5 ~ 6.8.8 · **커밋** `cd5157e`
