---
tags: [rag, retrieval, experiment, bm25, reranker, evaluation]
created: 2026-07-27
---

# 검색 실험 구도

BM25 하이브리드와 크로스인코더 리랭커를 **넣을지 말지 판단하기 위한** 실험 설계.
구조도는 [[검색 품질 개선 (BM25·리랭커)]], 여기까지 온 경위는 [[검색 개선 이력]].

핵심 전제 한 줄: **좋아 보이니까 넣는 게 아니라, 미리 정한 숫자를 넘겨야 넣는다.**

---

## 1. 왜 실험을 다시 설계했나

Plans 6.1에서 이미 6개 변형을 비교했는데 **`hit@5`가 전부 1.000**이었다. 검색을 바꿔도 숫자가 안 움직이니 무엇이 나은지 판단이 불가능.

원인은 지표가 **문서 단위**라는 것. 청킹이 모든 청크 앞에 `사업명+발주기관`을 붙이기 때문에 어떤 청크가 뽑히든 "정답 문서에서 뽑혔다"는 항상 참이 된다. 즉 지표가 검색기가 아니라 청킹 접두어를 재고 있었다.

> 그래서 실험 코드보다 **지표를 먼저** 새로 만들었다. 지표가 변별을 못 하면 그 뒤 실험은 전부 무의미하다.

---

## 2. 무엇을 비교하나 — 2×2 전수

BM25와 리랭커는 서로 독립적인 축이라, 하나씩 켜보는 게 아니라 **4칸을 전부** 돈다. 그래야 "둘 다 켜야만 좋아지는지 / 하나만으로 충분한지"가 갈린다.

| | 리랭커 OFF | 리랭커 ON |
|---|---|---|
| **BM25 OFF** | `A_dense` — 베이스라인 | `C_rerank` |
| **BM25 ON** | `B_hybrid` | `D_hybrid_rerank` |

- **A_dense** — 현행. e5-small 임베딩 검색 top-5
- **B_hybrid** — dense top-20 ⊕ BM25 top-20 을 RRF로 융합
- **C_rerank** — dense top-20 → 크로스인코더(bge-reranker-v2-m3)로 재배열 → top-5
- **D** — 둘 다

네 변형은 **같은 프로세스 안에서 config 플래그만 토글**해서 돈다. 인덱스·질문·정답이 전부 동일하므로 차이는 오직 검색 경로에서만 온다.

### 왜 RRF인가
코사인 유사도(0~1)와 BM25 점수(상한 없음)는 스케일이 달라 그냥 더할 수 없다. RRF는 **점수를 버리고 순위만** 쓴다.

```
score(청크) = Σ  1 / (60 + 그 검색기에서의 순위)
```

정규화 파라미터를 튜닝할 필요가 없어서, "융합 방식을 잘못 맞춰서 졌다"는 변명이 안 나온다.

---

## 3. 무엇으로 재나 — 순위 민감 지표

| 지표 | 보는 것 | 왜 필요 |
|---|---|---|
| **MRR@10** | 첫 정답이 몇 등에 있나 (`1/등수`) | **주 지표.** 1등↔3등 차이를 잡는다 |
| **nDCG@10** | 정답 여러 개가 위쪽에 몰렸나 | 정답이 여러 청크에 흩어진 문항용 |
| **Recall@5** | 실제 쓰이는 top-5 안에 몇 개 들어왔나 | 생성이 받는 컨텍스트의 실질 품질 |
| `hit@5` | (기존) 정답 문서가 뽑혔나 | **회귀 감시용으로만** — 1.0에서 안 떨어지는지 |

구현: `src/eval/ir_metrics.py`. 순수 함수 + stdlib만, 외부 의존 없음.

---

## 4. 무엇에 대고 재나 — 40문항

| 갈래 | 수 | 성격 |
|---|---|---|
| 기존 | 30 | 확장 골든셋에서 검색이 실제로 도는 문항만 추림 |
| 어휘형 | 10 | **이번 실험을 위해 새로 만든 문항** |
| **계** | **40** | 유형별: 단일세부 23 · 직답 13 · 후속 4 |

**어휘형 10문항을 따로 만든 이유** — BM25가 이기라고 만든 게 아니라, **BM25가 이겨야 정상인 구간이 실제로 있는지** 확인하려고. 고유번호(SFR-03)·법령 조항·정확한 기관명처럼 임베딩이 뭉개기 쉬운 표층 문자열을 묻는 질문들이다. 여기서도 BM25가 못 이기면 도입 근거가 없다.

### 보고는 3분할
전체 40 / 기존 30 / 어휘형 10 을 **따로** 낸다. 어휘형만 좋아지고 기존이 나빠지는 건 개선이 아니라 트레이드오프이고, 합쳐서 평균 내면 그게 숨는다.

---

## 5. 정답은 어떻게 정하나 — TREC 풀링

정답 청크를 사람이 처음부터 찾는 건 28,000개 중에서 고르는 일이라 불가능. 대신 **두 검색기가 건진 것만 후보로 놓고** 그중에서 고른다.

```
후보 = dense top-20  ∪  BM25 top-20     → 문항당 30~40개, 총 1,439개
```

실측 후보 분포 (재인덱싱 전 기준):

| | 후보 수 |
|---|---|
| dense만 찾음 | 411 |
| BM25만 찾음 | 411 |
| 둘 다 찾음 | 69 |

**겹침이 14%밖에 안 된다.** 두 문항은 겹침이 아예 0. → BM25가 dense가 못 보던 청크를 실제로 건져 올리고 있다 = **실험에 변별력이 있다.**

### 라벨링 3단계 (사람 부담 최소화)
1. 기계가 기준답변과 청크 본문의 **내용어 겹침**으로 후보를 점수화 → 상위 3개를 제안
2. 사람은 **엔터로 승인하거나 번호로 수정**만
3. 애매한 건 `v<번호>`로 전문을 펼쳐 확인

겹침 점수는 **질문이 아니라 기준답변**에 대고 잰다. 질문에 대고 재면 BM25가 찾기 쉬운 청크가 정답으로 뽑히는 **자기충족 편향**이 생긴다.

---

## 6. 언제 채택하나 — 승격 기준 (측정 전 확정)

> 변형이 베이스라인 A 대비
> **① 전체 문항 MRR@10 +0.05 이상** 개선하고
> **② 기존 문항 hit@5 = 1.0 유지**하면 → 프로덕션 반영 (config 플래그 ON)
>
> 하나라도 미달 → **미채택으로 기록**, 코드는 플래그 OFF로 남김
> 여러 변형이 통과하고 MRR 차이가 **0.02 이내면 더 단순한 쪽** (A < B,C < D)

**결과를 보기 전에 못 박은 이유** — 숫자를 본 뒤에 기준을 정하면 어떤 결과든 "성공"으로 해석할 수 있다. +0.03이 나왔을 때 "그래도 개선이니까"라고 말하지 않기 위한 사전 구속.

**더 단순한 쪽 우대**는 리랭커가 공짜가 아니기 때문. 568M 모델을 매 질의마다 20개 청크에 돌리는 비용·지연·GPU 메모리를, 0.02 차이로는 정당화할 수 없다.

---

## 7. 어디서 도나

GCP L4 GPU VM(`sprint-ai-11-2`). 리랭커는 CUDA fp16, **맥 MPS 경로는 쓰지 않는다** — 로컬과 VM의 수치가 갈리면 비교 자체가 무너진다.

OpenAI 호출 없음 → **API 비용 0**. 임베딩(로컬 e5)·BM25·리랭커 전부 로컬 모델이라 팀 키 $20 한도와 무관.

---

## 8. 이 실험이 답하지 못하는 것

세 가지는 결과 보고서에 반드시 함께 적는다.

1. **풀링의 태생적 한계** — 두 검색기 어느 쪽도 못 건진 청크는 영원히 정답이 될 수 없다. 실제 Recall은 여기서 재는 값보다 낮다.
2. **BM25는 승격만 시킬 수 있다** — BM25만 찾은 청크는 본문을 안 들고 있어 리랭킹 대상에서 빠진다. 즉 BM25의 기여는 "dense가 이미 본 청크를 위로 올리는 것"까지다. 진짜 신규 청크를 넣으려면 Chroma 왕복이 한 번 더 필요한데 v1 범위 밖.
3. **답변 품질은 안 잰다** — 이 실험은 검색 순위만 본다. 검색이 좋아져도 생성이 좋아진다는 보장은 없다(별도 골든셋 평가 몫).

---

## 9. 현재 상태

코드는 끝났다. 브랜치 `feat/hybrid-rerank`, 20커밋, 406테스트 통과, **플래그 기본 전부 OFF**(= 머지해도 현행 동작 그대로).

```
✅ 지표·하이브리드·리랭커 구현       (완료)
✅ 어휘형 10문항 작성·승인           (완료)
✅ VM 준비 (브랜치·패키지·L4 확인)   (완료)
⏳ 재인덱싱                          ← 지금 여기 (다른 세션)
□  후보 풀 재생성 (--pool)
□  정답 라벨링 40문항                ← 사람 작업
□  VM에서 4변형 측정
□  결과 기록 · 승격 판정 · PR
```

**재인덱싱을 먼저 하는 이유** — 청크 ID가 `{doc_id}::{순번}`이라 재인덱싱하면 전부 바뀐다. 라벨을 먼저 달면 전량 재매핑해야 한다. 지금은 라벨이 0건이라 잃을 게 없다.

---

## 확인 필요 (2026-07-27 발견)

- `data/goldenset.jsonl`이 **42문항**으로 늘어 있다. spec §7 / `GOLDENSET_MAX_ITEMS`는 상한 **30**. 다른 세션에서 확장본으로 교체된 것으로 보이는데, 정본 골든셋을 늘린 게 의도된 것인지 확인 필요. (평가 러너가 상한 검사를 하면 걸린다)
- 같은 파일이 `.gitignore`의 `data/` 규칙을 우회해 **git에 추적 중**이고 `origin/main`까지 올라가 있다 → NDA 관련 별도 판단 필요.

---

## 관련

[[검색 품질 개선 (BM25·리랭커)]] · [[검색 개선 이력]] · [[쿼리 파이프라인 개선안]] · [[온라인 쿼리 파이프라인]] · [[쿼리 파이프라인 개념 정리]]
